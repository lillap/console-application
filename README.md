# Node.js Console Application
A console application with 3 options:
1. Read a package.json file
2. Get OS info
3. Start an HTTP server 

The input must be a number between 1-3, otherwise an error message will be displayed, followed by the possibilty to make a new choice of input.  
